  version: 1
  type: pipeline-template

  name: integrate oc command
  description: deployment my app on openshift
  parameters:
    - name: github_repo
      type: string
      displayName: GitHub HTTPS repo endpoint
    - name: git-cred
      displayName: GitHub Credentials ID
      type: credentials
      defaultValue: github-creds-example
