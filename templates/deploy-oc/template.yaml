  version: 1
  type: pipeline-template

  name: deployment with oc CLI
  description: deployment on openshift
  parameters:
    - name: docker_repo
      type: string
      displayName: docker hub repo
    - name: image_name
      type: string
      displayName: image in docker hub   
    - name: app_name
      type: string
      displayName: the name of the application to deploy   


